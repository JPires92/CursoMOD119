@model CursoMOD119.ViewModels.Sales.SaleViewModel

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Sale</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="SaleDate" class="control-label"></label>
                <input asp-for="SaleDate" class="form-control" />
                <span asp-validation-for="SaleDate" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Amount" class="control-label"></label>
                <input asp-for="Amount" class="form-control" />
                <span asp-validation-for="Amount" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="ClientID" class="control-label"></label>
                <select asp-for="ClientID" class ="form-control" asp-items="ViewBag.ClientID"></select>
            </div>
@*            <div class="form-group">
                <label asp-for="ItemIDs" class="control-label"></label>
                <select asp-for="ItemIDs" class="form-control" asp-items="ViewBag.ItemIDs"></select>
            </div>*@
            <div class="form-group">
               <br /> <label asp-for="ItemIDs" class="control-label"></label>
                <table border="1">
                    <thead>
                        <tr>
                            <th class="col-1">@Html.DisplayNameFor(model => model.SelectableItems.First().Name)</th>
                            <th class="col-1">@Html.DisplayNameFor(model => model.SelectableItems.First().Price)</th>
                            <th class="col-1">@Html.DisplayNameFor(model => model.SelectableItems.First().Selected)</th>
                        </tr>
                    </thead>
                    <tbody>
                        @for( int i=0; i < Model.SelectableItems.Count; i++)
                        {
                            <tr>
                                <td>@Model.SelectableItems[i].Name</td>
                                <td>@Model.SelectableItems[i].Price</td>
                                <td><input asp-for="@Model.SelectableItems[i].Selected" /></td>
                                    <input type="hidden" asp-for="@Model.SelectableItems[i].ID">
                            </tr>
                        }

                    </tbody>
                </table>
                <br />
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
